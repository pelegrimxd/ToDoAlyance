# ToDoAlyance - Task Manager API

## 🎯 Продукт
Простой Task Manager с изоляцией задач между пользователями, CRUD операциями управления задачами и механизмом отслеживания просроченных задач.

## 🏗️ Архитектура проекта
```
backend/
├── api/                          # API слой (Controller)
│   ├── task_api.py              # Публичный API для пользователей
│   └── task_admin_api.py        # Админский API (просроченные задачи)
├── core/                        # Ядро приложения
│   └── settings.py              # Конфигурация
├── models/                      # Модели данных
│   ├── db_models/              # SQLAlchemy ORM модели
│   │   ├── base.py             # Базовый класс моделей
│   │   └── task.py             # Модель Task
│   ├── schemas/                # Pydantic схемы (DTO)
│   │   └── task.py             # Схемы для Task
│   └── static.py               # Константы, Enum
├── services/                    # Бизнес-логика (Service layer)
│   ├── domain/                 # Доменный слой
│   │   └── task_manager/       # Домен задач
│   │       ├── exception.py           # Доменные исключения
│   │       ├── task_manager_interface.py  # Интерфейс менеджера
│   │       └── task_manager_service.py    # Реализация логики
│   └── infrastructure/         # Инфраструктурный слой
│       └── task_database/      # Работа с БД
│           ├── task_database_interface.py  # Интерфейс репозитория
│           └── task_database_repository.py # Реализация репозитория
├── migration/                  # Alembic миграции
│   ├── versions/              # Файлы миграций
│   │   └── 31cc2b9e3f7b_initial_migration.py
│   ├── env.py                 # Конфигурация Alembic
│   └── script.py.mako         # Шаблон для миграций
└── dependencies.py            # FastAPI зависимости
main.py              # Точка входа FastAPI
```

**Стек:** Python 3.12 + FastAPI + SQLAlchemy + PostgreSQL + Docker + Poetry

## 🚀 Локальный запуск

### 1. Через Docker (проще)
```bash
# Клонировать репозиторий
git clone https://github.com/pelegrimxd/ToDoAlyance.git
cd ToDoAlyance

# Запустить
docker-compose up --build -d

# Запустить
docker exec -it fast_api alembic upgrade head
```

## 🔐 Аутентификация
Все запросы требуют заголовок:
```
X-User-Id: 1
```

## 📡 API Эндпоинты

<img width="770" height="460" alt="image" src="https://github.com/user-attachments/assets/5cf3628c-96ac-425d-9202-090074d2d013" />

## 🌐 Документация
После запуска доступна:
- **Swagger UI:** http://localhost:8000/docs
- **ReDoc:** http://localhost:8000/redoc
